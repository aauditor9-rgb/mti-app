// Drizzle table definitions go here.
// Every tenant-owned table carries `madrasah_id uuid not null references madrasah(id)`
// with row-level security enabled — see TECH_STACK.md "Multi-tenancy".
// Left empty until Stage 5 (connect the database).
export {};
