import type { NextConfig } from "next";

// Deployed on Vercel — set the function region to London (lhr1) in Project Settings
// per design/TECH_STACK.md "Hosting", so compute sits next to the Supabase eu-west-2 DB.
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
